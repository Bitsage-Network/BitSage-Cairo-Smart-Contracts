# Cairo/Starknet
/target/
*.profdata
.starknet_accounts/
deployed_contracts.json

# Deployment records (contain addresses & keystores)
/deployments/
testnet_*.json
*_deployment*.json
*_deployment*.log
temp_account.json

# Keystores (sensitive - NEVER commit)
**/keystores/*.json
**/keystore*.json
deployment/sepolia_keystore.json
deployment/pool_wallets/
admin_accounts/
airdrop/keystores/
*deployer*.json

# Deploy scripts with credentials (use environment variables instead)
deploy_*.mjs
sign_deploy*.mjs

# Environment files
.env
.env.local
.env.*.local
!.env.example
!.env.*.example

# Node
node_modules/
package-lock.json

# Backups
*.bak
*.bak2
*.backup

# OS
.DS_Store

# Test cache
.snfoundry_cache/
