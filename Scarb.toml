[package]
name = "sage_contracts"
version = "0.1.0"
edition = "2024_07"

[dependencies]
starknet = "2.12.0"
openzeppelin = { git = "https://github.com/OpenZeppelin/cairo-contracts", tag = "v0.20.0" }

# Herodotus Integrity - On-chain STARK proof verification
# Enables verifying STWO GPU proofs via FactRegistry
integrity = "2.1.0"

# Garaga - Efficient cryptographic operations for Groth16 (World ID)
# garaga = { git = "https://github.com/keep-starknet-strange/garaga", tag = "v0.15.0" }

[dev-dependencies]
snforge_std = { git = "https://github.com/foundry-rs/starknet-foundry", tag = "v0.54.0" }

[lib]

[[target.starknet-contract]]
casm = true

[tool.snforge]
name = "sage_contracts"
